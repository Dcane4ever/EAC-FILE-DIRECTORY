# Copy to .env (or set as real environment variables / server config) and fill in
# real values once the Google Cloud OAuth Client is created, restricted to eac.edu.ph.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
