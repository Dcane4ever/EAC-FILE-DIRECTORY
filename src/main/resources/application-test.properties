# Local click-through testing profile ONLY. Activate with:
#   ./mvnw spring-boot:run -Dspring-boot.run.profiles=test
# Swaps the real email+password login for a one-click @eac.edu.ph email form
# at /dev-login (see ph.edu.eac.filedirectory.devauth) - skips registration
# and email verification entirely. Everything else (DB, storage, taxonomy
# seed, upload/browse/admin flows) is identical to the real app. Never
# activate this profile on a deployed server.
